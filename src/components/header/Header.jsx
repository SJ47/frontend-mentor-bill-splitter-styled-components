import React from "react";
import { StyledHeader } from "./Header.styled";

const Header = () => {
    return (
        <StyledHeader>
            <h2>
                SPLI
                <br />
                TTER
            </h2>
        </StyledHeader>
    );
};

export default Header;
